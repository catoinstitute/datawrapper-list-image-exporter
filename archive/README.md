# Earlier versions

Preserved for reference; active development belongs in the root `index.html`.

- `chart-resizer-v1.html`: original `index.html`, before the integrated chart finder.
- `chart-finder-prototype.html`: original `v2-finder-prototype.html`, with simulated finder results.
- `chart-panel-bookmarklet.txt`: earlier standalone bookmarklet source and installation notes. Its localhost URL and encoded bookmarklet are historical; install the current bookmarklet from the main app instead.
